# Whenever RSS live test

This public repository provides an editable GitHub Atom feed for testing Whenever's `feed.item.published` trigger.

The commits on `main` are the feed items. Existing commits seed the feed; a new commit made after activation should create one event run.
